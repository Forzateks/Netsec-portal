# Archive

This folder holds historical snapshots and import artifacts that should stay out of the live app surface.

## Archived Snapshot

- `V1.3-04-04-2026/`: copied from the previous root-level snapshot for historical reference

## Notes

- The runtime app still uses the root `index.html`, `css/`, and `js/` files
- Historical SQL, spreadsheets, and static exports belong here rather than beside production entry files
